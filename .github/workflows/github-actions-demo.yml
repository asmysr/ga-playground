# TODO: Allocate different deployment strategy
# TODO: Deploy only if conditions are met
# Multi-Stage Deployments are inside the Github Action settings

name: GitHub Actions Demo On The Playground
on: 
    # push:
    #   branches: [ main ]
    # pull_request:
    #   branches: [ main ]
    workflow_dispatch:
      branches:
        - develop
    
jobs:
  deploy:
    name: deploy to staging
    environment:
      name: Staging Environment # can set the environment here
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: npm install
         run: npm install
      - name: decrypt secrets
         run: ./.github/scripts/decrypt_secret.sh
         env:
           SECRET_PASSPHRASE: ${{secrets.SECRET_PASSPHRASE}}
      - name: Serverless deployment to staging
        uses: serverless/github-action@v1.53.0
        with:
         args: deploy
        env:
         SERVERLESS_ACCESS_KEY: ${{secrets.SERVERLESS_ACCESS_KEY}}
         CREDENTIALS_PATH: /github/workspace/secrets/keyfile.json
